# tags: 
openapi: 3.0.0
info:
  title: Sample API
  description: A sample API that uses trailing slashes.
  version: 1.0.0
servers:
  - url: https://api.example.com/v1
paths:
  /foo:
    get:
      summary: Summary 1
      description: Description 1
      responses:
        "200":
          description: Response description 1
          content:
            text/plain:
              schema:
                type: string
  /foo/:
    get:
      summary: Summary 2
      description: Description 2
      responses:
        "200":
          description: Response description 2
          content:
            text/plain:
              schema:
                type: string
